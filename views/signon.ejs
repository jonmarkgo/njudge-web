<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dip Adjudicator - Sign On</title>
    <style>
        /* Add similar styling as interface.ejs */
        body { font-family: sans-serif; line-height: 1.6; padding: 20px; max-width: 900px; margin: auto; }
        .container { display: flex; gap: 20px; flex-wrap: wrap;}
        .action-section { flex: 1; min-width: 350px; border: 1px solid #eee; padding: 15px; margin-bottom: 15px; background-color: #fdfdfd;}
        .output-section { flex: 1; min-width: 350px; }
        label { display: block; margin-bottom: 5px; font-weight: bold; }
        select, input[type="text"], input[type="password"], textarea {
            width: 100%; padding: 8px; margin-bottom: 10px; box-sizing: border-box; border: 1px solid #ccc;
        }
        textarea { min-height: 100px; font-family: monospace; }
        #output { background-color: #f4f4f4; border: 1px solid #ddd; padding: 10px; white-space: pre-wrap; word-wrap: break-word; min-height: 200px; max-height: 500px; overflow-y: auto; font-family: monospace; font-size: 0.9em; }
        button { padding: 10px 15px; cursor: pointer; margin-top: 10px; }
        h2 { border-bottom: 1px solid #ccc; padding-bottom: 5px; margin-top: 0;}
        .user-info { background-color: #eef; padding: 5px 10px; margin-bottom: 15px; border-radius: 4px; display: flex; justify-content: space-between; align-items: center;}
        .command-options { border-top: 1px dashed #eee; padding-top: 10px; margin-top: 10px;}
        .command-options label { font-weight: normal; }
        .command-options input, .command-options textarea { margin-bottom: 5px; }
    </style>
</head>
<body>
    <h1>Dip Adjudicator - Pre-Game Actions</h1>
    <div class="user-info">
        <span>Session for: <strong><%= email %></strong></span>
        <form action="/signoff" method="POST" style="margin: 0;">
             <button type="submit">Sign Out</button>
        </form>
    </div>

    <div class="container">
        <div class="action-section">
            <h2>Available Actions</h2>
            <p>Register if needed, list available games, or sign on.</p>

            <label for="command-type">Select Action:</label>
            <select id="command-type">
                <option value="">-- Select --</option>
                <option value="REGISTER">REGISTER</option>
                <option value="LIST">LIST Games</option>
                <option value="SIGN ON ?">SIGN ON ? (Next Available)</option>
                <option value="SIGN ON ?game">SIGN ON ?game (Specific Forming)</option>
                <option value="SIGN ON power">SIGN ON power (Existing/Abandoned)</option>
                <option value="OBSERVE">OBSERVE Game</option>
                <option value="GET">GET File (e.g., info, rules)</option>
                <option value="WHOIS">WHOIS User</option>
                 <option value="MANUAL">Manual Command</option>
            </select>

            <!-- Dynamic Options Area -->
            <div id="command-options-area" class="command-options"></div>

            <label for="generated-command">Generated Command / Manual Input:</label>
            <textarea id="generated-command" placeholder="Select action above or type command here. Do NOT include SIGN OFF."></textarea>

            <button id="send-command">Send Command</button>
             <p style="font-size: 0.8em; color: #666; margin-top: 15px;">Note: After a successful 'SIGN ON power', you may need to refresh this page or it might refresh automatically.</p>
        </div>

        <div class="output-section">
            <h2>Adjudicator Output</h2>
            <div id="output">Output will appear here...</div>
        </div>
    </div>

    <!-- Use the same client script, it will handle the elements present -->
    <script src="/js/client.js"></script>
</body>
</html>