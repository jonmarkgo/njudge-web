<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NJudge Web Interface</title>
    <link href="/css/styles.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        /* Add minor style adjustments if needed */
        #game-selector { min-width: 150px; }
    </style>
</head>
<body class="min-h-screen bg-gray-50 flex flex-col">
    <nav class="bg-white shadow-sm">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex">
                    <div class="flex-shrink-0 flex items-center">
                        <a href="/" class="text-xl font-bold text-primary">NJudge</a>
                    </div>
                </div>
                <% if (typeof user !== 'undefined' && user) { %>
                    <div class="flex items-center">
                        <span id="user-email-indicator" class="text-gray-700 mr-4" data-email="<%= user %>"><%= user %></span>
                        <form action="/signoff" method="POST" class="m-0">
                             <button type="submit" class="btn btn-secondary text-sm py-1 px-3">Sign Out</button>
                        </form>
                    </div>
                <% } %>
            </div>
        </div>
    </nav>

    <main class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8 flex-grow w-full">
        <%- body %>
    </main>

    <footer class="bg-white border-t mt-auto">
        <div class="max-w-7xl mx-auto py-4 px-4 sm:px-6 lg:px-8">
            <p class="text-center text-gray-500 text-sm">
                © <%= new Date().getFullYear() %> NJudge Web Interface. Diplomacy™ is a trademark of Hasbro, Inc. Adjudicator software by Ken Lowe & njudge project.
            </p>
        </div>
    </footer>

    <!-- Add client.js script -->
    <%- defineContent('scripts') %>
    <script src="/js/client.js"></script>
</body>
</html>